<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/iv_recent_search_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_history"
        app:tint="#757575"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="12dp"
        android:contentDescription="최근 검색 아이콘"/>

    <TextView
        android:id="@+id/tv_recent_search_query"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/iv_recent_search_icon"
        android:layout_toStartOf="@+id/btn_delete_recent_search_item"
        android:layout_centerVertical="true"
        android:text="최근 검색어"
        android:textColor="#333333"
        android:textSize="14sp"
        android:maxLines="1"
        android:ellipsize="end"/>

    <ImageButton
        android:id="@+id/btn_delete_recent_search_item"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_close_small"
        app:tint="#9E9E9E"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="8dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:contentDescription="개별 검색어 삭제"/>
</RelativeLayout>
